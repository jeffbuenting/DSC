@{
    AllNodes = @(
        @{
            NodeName = 'JB-CRM01' 
            Role = 'CRM'
        },

        @{
            NodeName = 'JB-SQL01'
            Role = 'SQL'
        }
    );

    NonNodeData = ''
}